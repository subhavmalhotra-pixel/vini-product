# ROI Tracker — Supabase read config.
# When BOTH are set, the tracker reads live roi_digest_runs + engagement.
# When unset, it falls back to mock data (src/tracker/mockData.ts).
# ⚠️ anon key + no auth exposes dealer PII — see db/SUPABASE-REVIEW.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
