[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — react-router needs every route to return index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
